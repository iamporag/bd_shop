import 'package:flutter/material.dart';

class ExploreReview extends StatelessWidget {
  const ExploreReview({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}